# Database (SQLite by default). For Postgres:
#   DATABASE_URL="postgresql://user:pass@host:5432/sprout"
DATABASE_URL="file:./sprout.db"

# Self-hosted push notifications via ntfy (optional — leave blank to disable)
NTFY_URL=
NTFY_TOPIC=
NTFY_TOKEN=

# Node
NODE_ENV=development
PORT=3000
